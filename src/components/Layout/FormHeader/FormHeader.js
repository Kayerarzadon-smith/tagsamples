import React from 'react';
import css from './FormHeader.css';

const formheader = (props) => (
    <div className={css.formheader}>
        <h2>Upload Your Logo Here </h2>
    </div>
);

export default formheader;